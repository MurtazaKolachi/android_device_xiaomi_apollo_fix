# Allow system server battery suply rw access
allow system_server vendor_sysfs_battery_supply:file rw_file_perms;
# Allow system_server (PerformanceBooster) to execute the 'am' command
allow system_server shell_exec:file { read open execute getattr };
